<?xml version="1.0" encoding="UTF-8"?>

<!--suppress SpringPlaceholdersInspection -->
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
                            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd">


    <bean id="activitySubscriberManager" class="com.max.services.impl.ActivityQueueManager">
        <property name="topicSettings">
            <map>
                <entry key="DataIntegrity" value-ref="dataIntegrityTopicSettings"/>
                <entry key="UserActivity" value-ref="userActivityTopicSettings"/>
            </map>
        </property>
    </bean>

<!--
    <bean id="mailService" class="com.max.services.impl.MailServiceTestImpl">
    </bean>
-->
<!--
        <bean id="mailService" class="com.max.services.impl.MailServiceImpl">
            <property name="mailhost" value="${smtp.host}"/>
            <property name="errorEmailAddress" value="${smtp.errors.recipient}"/>
            <property name="password" value="${smtp.auth.password}"/>
            <property name="username" value="${smtp.auth.username}"/>
            <property name="senderEmail" value="${smtp.default.sender.email}"/>
            <property name="senderName" value="${smtp.default.sender.name}"/>
        </bean>
-->


    <!-- If there are ever any service that require some global configurable value, those would be injected here -->

</beans>
